# Demo-ready environment variables for the friendly receptionist speaking demo.
# Copy to `.env` before running the demo scripts.

# Daily.co room that SpotFunnel operates (this is the provided demo room)
DAILY_ROOM_URL=https://spotfunnel.daily.co
DAILY_ROOM_TOKEN=6171fc7b8774c059be5176b3c3c75274775b93b454641168c2ca575bebb4f3cf

# Deepgram STT
DEEPGRAM_API_KEY=8649f618a3f1e149e928bf3aaf58b73e2bceb536

# AssemblyAI STT (multi-ASR)
ASSEMBLYAI_API_KEY=your_assemblyai_api_key

# Google Gemini LLM (primary - production standard Feb 2026)
GOOGLE_API_KEY=your_google_api_key
GEMINI_MODEL=gemini-2.5-flash

# OpenAI LLM (fallback)
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4o
OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe
OPENAI_RANKER_MODEL=gpt-4o-mini

# ElevenLabs TTS (fallback)
ELEVENLABS_API_KEY=your_elevenlabs_api_key
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Cartesia TTS (primary, used when MultiProviderTTS is enabled)
CARTESIA_API_KEY=your_cartesia_api_key
CARTESIA_VOICE_ID=a0e99841-438c-4a64-b679-ae501e7d6091
CARTESIA_MODEL=sonic-3

# ========================================
# Smart Turn V3 Configuration (Turn Detection)
# ========================================
# Smart Turn uses ML-based turn detection to reduce premature cutoffs.
# Works in conjunction with Silero VAD (VAD detects silence, Smart Turn classifies turn completion).
#
# Model: smart-turn-v3.2-cpu (8MB, 12ms inference, 23 languages)
# Research: voice-ai-os/research/01-turn-taking.md
# Upstream: https://github.com/pipecat-ai/smart-turn

# Enable Smart Turn V3 (default: true)
SMART_TURN_ENABLED=true

# Path to custom Smart Turn ONNX model (optional, uses bundled model if not set)
# SMART_TURN_MODEL_PATH=/path/to/smart-turn-v3.2-cpu.onnx

# Number of CPUs for Smart Turn inference (default: 1)
# Note: 12ms inference on modern CPUs with single core
SMART_TURN_CPU_COUNT=1
